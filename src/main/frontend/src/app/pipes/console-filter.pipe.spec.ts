import { ConsoleFilterPipe } from './console-filter.pipe';

describe('ConsoleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsoleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
